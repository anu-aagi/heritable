# Check whether the fitted model contains random terms not grouped by `target`

Check whether the fitted model contains random terms not grouped by
`target`

## Usage

``` r
check_all_random_terms_match_target(model, target)
```
