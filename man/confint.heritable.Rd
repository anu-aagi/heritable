% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/confint.R
\name{confint.heritable}
\alias{confint.heritable}
\title{Bootstrap confidence interval for heritability}
\usage{
\method{confint}{heritable}(
  heritable,
  B = 100,
  seed = NULL,
  resample_u = TRUE,
  level = 0.95,
  type = c("basic", "norm", "perc"),
  ...
)
}
\arguments{
\item{heritable}{A heritability object returned by \code{heritable::H2()} (broad-sense) or
\code{heritable::h2()} (narrow-sense). The object must store the fitted model
as an attribute.}

\item{B}{Integer. Number of bootstrap replicates.}

\item{seed}{Optional random seed.}

\item{resample_u}{Logical. Whether to resample random effects in the bootstrap.}

\item{level}{Confidence level.}

\item{type}{Character. Bootstrap interval type; one of \code{"basic"},
\code{"norm"}, or \code{"perc"}.}

\item{...}{Additional arguments passed to the bootstrap routine.}
}
\value{
A confidence interval object as returned by \code{confint()}.
}
\description{
Computes a confidence interval for a heritability estimate using parametric
bootstrap of the underlying mixed model.
}
\examples{
lettuce_subset <- lettuce_phenotypes |> subset(loc == "L2")
lettuce_asreml <- asreml(
  fixed = y ~ rep * pseudo_var1,
  random = ~gen,
  sparse = ~pseudo_var2,
  data = lettuce_subset,
  trace = FALSE
)

my_H2 <- H2(lettuce_asreml, "gen", c("Cullis", "Standard"))

confint(my_H2)

}
