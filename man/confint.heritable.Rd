% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/confint.R
\name{confint.heritable}
\alias{confint.heritable}
\title{Bootstrap confidence interval for heritability}
\usage{
\method{confint}{heritable}(
  object,
  parm = NULL,
  level = 0.95,
  B = 100,
  random_effect = c("resample", "fix"),
  type = c("basic", "norm", "perc"),
  return_model = TRUE,
  seed = NULL,
  ...
)
}
\arguments{
\item{object}{A heritability object returned by \code{\link[=H2]{H2()}} (broad-sense) or
\link[=H2]{heritable::h2()} (narrow-sense). The object must store the fitted model
as an attribute.}

\item{parm}{a specification of which parameters are to be given confidence intervals,
either a vector of numbers or a vector of names.
If missing, all parameters are considered.}

\item{level}{Confidence level.}

\item{B}{Integer. Number of bootstrap replicates.}

\item{random_effect}{Character. Indicate whether to resample random
effect; one of \code{"resample"} or \code{"fix"}.}

\item{type}{Character. Bootstrap interval type; one of \code{"basic"},
\code{"norm"}, or \code{"perc"}.}

\item{return_model}{Logical. Whether to return to the \code{boot} object.}

\item{seed}{Optional random seed.}

\item{...}{Additional arguments passed to the bootstrap routine.}
}
\value{
A matrix of confidence intervals.
}
\description{
Computes a confidence interval for a heritability estimate using parametric
bootstrap of the underlying mixed model.
}
\examples{
\dontrun{
  lettuce_subset <- lettuce_phenotypes |> subset(loc == "L2")
  lettuce_asreml <- asreml(
    fixed = y ~ rep * pseudo_var1,
    random = ~gen,
    sparse = ~pseudo_var2,
    data = lettuce_subset,
    trace = FALSE
  )

  my_H2 <- H2(lettuce_asreml, "gen", c("Cullis", "Standard"))

  confint(my_H2)
}

}
