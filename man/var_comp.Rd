% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{var_comp}
\alias{var_comp}
\title{Extract variance components}
\usage{
var_comp(
  model,
  target,
  calc_C22 = TRUE,
  calc_V = TRUE,
  calc_C11 = TRUE,
  marginal = TRUE,
  stratification = NULL,
  solver = c("LMM", "direct"),
  ...
)
}
\arguments{
\item{model}{Model object of class \code{lmerMod/merMod} or \code{asreml}}

\item{target}{The name of the random effect for which heritability is to be calculated.}

\item{calc_C22}{Logical; whether to compute the prediction error
variance (PEV) matrix for the transformed target effect.}

\item{calc_V}{Logical; whether to retain the marginal variance
matrix of the response and supporting quantities (\code{V}, \code{G}, \code{Z}, \code{X}, \code{idx},
and \code{m}) in the output.}

\item{calc_C11}{Logical; whether to compute the variance
matrix of the fixed-effect counterpart estimator for the transformed target
effect.}

\item{marginal}{Logical; if \code{TRUE}, construct marginal (strata-averaged)
mappings so that each genotype receives a single averaged effect per term.
If \code{FALSE}, mappings will only consider the main genotype effect and ignore the
iteracting terms.}

\item{stratification}{A one-row data frame defining the stratum in which
genotype effects should be evaluated. The columns must correspond
to model terms that interact with \code{target}.}

\item{solver}{A string specifying the solver for the PEV matrix. Can be
either \code{"direct"} (directly invert \code{V}) or \code{"LMM"} (Solve the LMM equation).}

\item{...}{Additional arguments passed to downstream helper functions.}
}
\value{
A named list with the following elements:

\describe{
\item{\code{n_g}}{Number of transformed genetic coefficients after applying the
mapping matrix \code{m}.}
\item{\code{gnames}}{Names of the transformed genetic coefficients.}
\item{\code{G_g}}{Variance matrix of the transformed target genetic effect.}
\item{\code{C22_g}}{Prediction error variance matrix of the transformed target
effect, if \code{calc_C22 = TRUE}; otherwise \code{NULL}.}
\item{\code{G_g_tilde}}{Variance matrix of the fixed-effect counterpart estimator for
the transformed target effect, if \code{calc_C11 = TRUE}; otherwise \code{NULL}.}
\item{\code{G_g_tilde_no_cov}}{Variance matrix of the fixed-effect counterpart estimator for
the transformed target effect, without considering target covariance, if \code{calc_C11 = TRUE}; otherwise \code{NULL}.}
\item{\code{C11_g}}{Estimation error variance matrix of the fixed-effect counterpart estimator for
the transformed target effect, if \code{calc_C11 = TRUE}; otherwise \code{NULL}.}
\item{\code{V}}{Marginal covariance matrix of the response, if \code{calc_V = TRUE};
otherwise \code{NULL}.}
\item{\code{G}}{Variance matrix of all random effects, if \code{calc_V = TRUE};
otherwise \code{NULL}.}
\item{\code{Z}}{Random-effect design matrix, if \code{calc_V = TRUE}; otherwise \code{NULL}.}
\item{\code{X}}{Fixed-effect design matrix, if \code{calc_V = TRUE}; otherwise \code{NULL}.}
\item{\code{idx}}{Indices of the random-effect coefficients associated with the
target term, if \code{calc_V = TRUE}; otherwise \code{NULL}.}
\item{\code{W}}{Linear mapping from the original target coefficients to the
transformed target effect, if \code{calc_V = TRUE}; otherwise \code{NULL}.}
\item{\code{marginal}}{Logical scalar indicating whether the returned quantities
correspond to a marginal definition of the target effect.}
\item{\code{stratification}}{The user-supplied stratification object, if any.}
}
}
\description{
This function provides the variance matrix quantities needed to evaluate heritability
with respect to a target random-effect term in a fitted linear mixed model.
}
